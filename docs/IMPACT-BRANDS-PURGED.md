# impact-brands-site — retired 2026-06-08

Harvested into `viss-tool's/preset_libraries/` (animations + `elements/impact-brands/`).

Mirror removed. If an empty `impact-brands-site/server/` folder remains, Windows has a cwd lock — delete after **Reload Window** or reboot:

```powershell
Remove-Item -LiteralPath "...\website_templates\impact-brands-site" -Recurse -Force
```

Port **5190** is no longer used.
