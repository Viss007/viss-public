You are on Cursor Cloud VM. Repo: Viss007/viss-public (main). App folder: agents/docs_agent (not a separate repo).

=== WHAT THIS VIDEO IS ===
Screen recording of the **documentation agent app** — standalone invoice PDF → AI extraction → review table → Excel export.
Employers watch this MP4 inside the portfolio "Watch walkthrough" panel. The video must show **only the app**, not the hire-me site.

=== SECRETS (required for live extraction on camera) ===
OPENAI_API_KEY must be in Cloud Secrets (global is fine). start.sh writes agents/docs_agent/.env. Never paste keys in chat.

=== PHASE A — run docs-agent only (:3000) ===
1) Wait for environment install (.cursor/environment.json runs npm ci in agents/docs_agent).
2) Start: cd agents/docs_agent && chmod +x start.sh && ./start.sh
3) Pre-check: curl -s http://127.0.0.1:3000/health → ok:true, vision:true

=== BANNED (do not record, do not save) ===
- Cursor RecordScreen / desktop screen capture (adds Cursor watermark, shows dock/desktop/browser chrome).
- http://127.0.0.1:3333/agents.html or any hire-me portfolio chrome.
- Home / hub / about / teams / live-projects / Automations tab.
- Half-screen browser or padded length — keep video as long as the flow needs (~15–30s is fine).

=== CORRECT URL ===
http://127.0.0.1:3000/ — page title **Invoices → Excel**

=== RECORD (preferred — viewport only, professional) ===
From repo root:

  cd scripts && npm init -y && npm install playwright@1.49.1
  npx playwright install chromium
  node record-docs-agent-kiosk.mjs

Records ONLY the 1920×1080 app viewport (no browser chrome, no desktop, no Cursor logo).
Output: website-templates/public/videos/demos/docs-agent.mp4

Automated flow: Sample invoice 1 → Process invoices → Review table → Notes edit → Export Excel.

=== GIT ===
git add website-templates/public/videos/demos/docs-agent.mp4 scripts/record-docs-agent-kiosk.mjs
git commit -m "Replace docs-agent walkthrough with viewport app demo."
git push origin main

=== END REPORT ===
Return: duration, file size, commit hash; confirm viewport-only; confirm sample → process → table → Excel succeeded.

Rules: no .env or API keys in chat. Demo only — no mass send.
