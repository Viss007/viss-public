# Public automations lane — frozen (relocation only)

**Locked:** 2026-06-08 (operator)

## What is frozen

- New **3333** features (Automations tab cards, Laravel proxies, new loopback children in `start-public.bat`) unless Viss explicitly unfreezes.
- Final taxonomy of what belongs on **3333** vs **9872** — blocked until **`viss-workspace`** consolidation is further along.

## What is still allowed

- **Relocate** misplaced connectors into `Public/automations/` (copy + tombstone in workspace).
- **Bugfixes** on already-live proxies (MCP **2091**, speed-to-lead **3001**) if something is broken — not new product surface.
- **Gmail OAuth** stays frozen separately — `automations/speed_to_lead/GMAIL-OAUTH-FROZEN.md`.

## Thaw

Say **`unfreeze public automations`** to resume active **3333** automations product work.

**SSOT:** `docs/BOUNDARIES.md` (build order + taxonomy).
