# Fastify gateway — removed from Public

**2026-06-05:** Hire-me site = **one runtime, one port** (**3333**, `dev-server.mjs`). The separate Fastify **`gateway/`** on **3040** is **retired** — no proxy to `vissai_platform` demo ports.

If **`Desktop\Public\gateway`** still exists on disk (folder lock), delete it manually when nothing holds the path open.

**Platform demos:** `viss-workspace\vissai_platform\Start-VissAI-Platform.bat` — separate from Public.
